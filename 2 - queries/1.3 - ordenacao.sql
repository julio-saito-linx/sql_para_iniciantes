select * from ingrediente;
-- ordenar pelo nome crescente
select * from ingrediente order by nome;
-- ordenar pelo nome decrescente
select * from ingrediente order by nome desc;
-- ordenar pelo id
select * from ingrediente order by id;
